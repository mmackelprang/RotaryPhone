@inject RotaryPhoneController.GVBridge.Adapters.GVApiAdapter Adapter

<div style="margin-bottom: 1rem; padding: 1rem; border: 1px solid #ccc; border-radius: 8px;">
    <h3>GV API Bridge Status</h3>
    <p>
        API:
        <span style="color: @(Adapter.IsAvailable ? "green" : "red"); font-weight: bold;">
            @(Adapter.IsAvailable ? "Available" : "Unavailable")
        </span>
    </p>
</div>
